<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.attendance.app.mapper.AuditLogMapper">
    <resultMap id="AuditLogResultMap" type="com.attendance.app.entity.AuditLog">
        <id property="logId" column="log_id" />
        <result property="eventType" column="event_type" />
        <result property="actorUserId" column="actor_user_id" />
        <result property="targetUserId" column="target_user_id" />
        <result property="targetType" column="target_type" />
        <result property="targetId" column="target_id" />
        <result property="description" column="description" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <select id="findById" resultMap="AuditLogResultMap">
        SELECT * FROM audit_logs WHERE log_id = #{id}
    </select>

    <select id="findAll" resultMap="AuditLogResultMap">
        SELECT * FROM audit_logs
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="logId">
        INSERT INTO audit_logs (event_type, actor_user_id, target_user_id, target_type, target_id, description, created_at)
        VALUES (#{eventType}, #{actorUserId}, #{targetUserId}, #{targetType}, #{targetId}, #{description}, #{createdAt})
    </insert>

</mapper>
