<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.attendance.app.mapper.UserNotificationMapper">

    <resultMap id="notificationResultMap" type="com.attendance.app.entity.UserNotification">
        <id column="notification_id" property="notificationId"/>
        <result column="user_id" property="userId"/>
        <result column="message" property="message"/>
        <result column="is_read" property="isRead"/>
        <result column="notification_type" property="notificationType"/>
        <result column="sender_user_id" property="senderUserId"/>
        <result column="sender_name" property="senderName"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"
                typeHandler="com.attendance.app.config.typehandler.InstantTypeHandler"/>
    </resultMap>

    <resultMap id="notificationKeyResultMap" type="com.attendance.app.dto.UserNotificationKeyDto">
        <result column="user_id" property="userId"/>
        <result column="notification_type" property="notificationType"/>
    </resultMap>

    <select id="selectUnreadByUserId" resultMap="notificationResultMap">
        SELECT un.notification_id,
               un.user_id,
               un.message,
               un.is_read,
               un.notification_type,
               un.sender_user_id,
               u.full_name AS sender_name,
               un.created_at
          FROM user_notifications un
          LEFT JOIN users u ON un.sender_user_id = u.user_id
         WHERE un.user_id = #{userId}
           AND un.is_read = false
         ORDER BY un.created_at DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="notificationId">
        INSERT INTO user_notifications (user_id, message, notification_type, is_read, sender_user_id, created_at)
        VALUES (
            #{userId},
            #{message},
            COALESCE(#{notificationType}, 'REMINDER'),
            false,
            #{senderUserId},
            NOW()
        )
    </insert>

    <update id="markAsRead">
        UPDATE user_notifications
           SET is_read = true
         WHERE notification_id = #{notificationId}
           AND user_id          = #{userId}
    </update>

    <select id="selectExistingNotificationKeys" resultMap="notificationKeyResultMap">
        SELECT DISTINCT user_id, notification_type
          FROM user_notifications
         WHERE user_id IN
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
           AND notification_type IN
        <foreach collection="notificationTypes" item="notificationType" open="(" separator="," close=")">
            #{notificationType}
        </foreach>
           AND created_at &gt;= #{since, typeHandler=com.attendance.app.config.typehandler.InstantTypeHandler}
    </select>

    <update id="markAllAsRead">
        UPDATE user_notifications
           SET is_read = true
         WHERE user_id = #{userId}
           AND is_read = false
    </update>

</mapper>
